<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Upload Voters Form</title>
  </head>
  <body>
    <div class="w-full max-w-2xl mx-auto bg-white rounded-2xl shadow-2xl">
      <!-- Header -->
      <div class="flex justify-between items-center p-6 border-b border-gray-200 bg-gradient-to-r from-purple-600 to-purple-700">
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 bg-white rounded-lg flex items-center justify-center">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-purple-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
            </svg>
          </div>
          <h2 class="text-xl sm:text-2xl font-bold text-white">Upload Voters (Excel)</h2>
        </div>
        <button
          onclick="removeUploadVotersForm()"
          class="modal-close-btn text-white hover:text-red-200 text-3xl font-bold leading-none transition-all"
          aria-label="Close"
          type="button"
        >
          &times;
        </button>
      </div>

      <!-- Form -->
      <form id="upload-voters-form" class="p-6 space-y-4">
        
        <!-- Instructions -->
        <div class="bg-gradient-to-r from-purple-50 to-indigo-50 border-l-4 border-purple-500 p-4 rounded-r-xl">
          <div class="flex items-start">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-purple-500 mt-0.5 mr-3 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <div>
              <h3 class="text-sm font-semibold text-purple-900 mb-2">Excel File Requirements</h3>
              <ul class="text-xs text-purple-800 space-y-1 list-disc list-inside">
                <li>File must be in .xlsx or .xls format</li>
                <li>Required columns: <strong>firstName</strong>, <strong>lastName</strong>, <strong>voterId</strong></li>
                <li>Optional column: <strong>phone_number</strong></li>
                <li>First row should contain column headers</li>
                <li>Maximum file size: 10MB</li>
              </ul>
            </div>
          </div>
        </div>

        <!-- File Upload -->
        <div>
          <label for="votersFile" class="block text-sm font-semibold text-gray-700 mb-2">
            Select Excel File <span class="text-red-500">*</span>
          </label>
          <div class="relative">
            <input
              type="file"
              name="votersFile"
              id="votersFile"
              accept=".xlsx,.xls"
              required
              class="hidden"
            />
            <label
              for="votersFile"
              class="flex flex-col items-center justify-center w-full px-4 py-8 bg-gray-50 border-2 border-dashed border-gray-300 rounded-xl cursor-pointer hover:bg-gray-100 hover:border-purple-500 transition-all"
            >
              <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 text-gray-400 mb-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
              <span class="text-gray-600 text-sm font-medium">Click to select Excel file</span>
              <span class="text-gray-400 text-xs mt-1">or drag and drop</span>
            </label>
            <div id="file-name-upload" class="mt-3 text-sm text-gray-600 text-center hidden"></div>
          </div>
        </div>

        <!-- Sample Template Download -->
        <div class="bg-blue-50 border border-blue-200 rounded-xl p-4">
          <div class="flex items-center justify-between">
            <div class="flex items-center gap-3">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
              <div>
                <p class="text-sm font-semibold text-blue-900">Need a template?</p>
                <p class="text-xs text-blue-700">Download our sample Excel file</p>
              </div>
            </div>
            <a 
              href="/templates/voters-template.xlsx" 
              download
              class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-xs font-semibold transition-all"
            >
              Download
            </a>
          </div>
        </div>

        <!-- Action Buttons -->
        <div class="flex flex-col-reverse sm:flex-row justify-end gap-3 pt-4 border-t border-gray-200">
          <button
            type="button"
            onclick="removeUploadVotersForm()"
            class="w-full sm:w-auto px-6 py-3 bg-gray-200 hover:bg-gray-300 text-gray-700 rounded-xl font-semibold transition-all text-sm sm:text-base"
          >
            Cancel
          </button>
          <button
            type="submit"
            class="w-full sm:w-auto px-6 py-3 bg-gradient-to-r from-purple-600 to-purple-700 hover:from-purple-700 hover:to-purple-800 text-white rounded-xl font-semibold transition-all text-sm sm:text-base shadow-lg flex items-center justify-center gap-2"
          >
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
            </svg>
            <span>Upload & Import</span>
          </button>
        </div>
      </form>
    </div>

    <!-- <script>
      // Show file name when selected
      document.getElementById('votersFile').addEventListener('change', function(e) {
        const fileName = e.target.files[0]?.name;
        const fileNameDiv = document.getElementById('file-name-upload');
        if (fileName) {
          fileNameDiv.innerHTML = `
            <div class="flex items-center justify-center gap-2 bg-purple-100 border border-purple-300 rounded-lg p-3">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-purple-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
              <span class="font-medium text-purple-900">${fileName}</span>
            </div>
          `;
          fileNameDiv.classList.remove('hidden');
        } else {
          fileNameDiv.classList.add('hidden');
        }
      });

      document.getElementById('upload-voters-form').addEventListener('submit', async function(e) {
        e.preventDefault();
        
        const submitBtn = this.querySelector('button[type="submit"]');
        const originalHTML = submitBtn.innerHTML;
        submitBtn.disabled = true;
        submitBtn.innerHTML = `
          <svg class="animate-spin h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
          </svg>
          <span>Uploading...</span>
        `;

        const formData = new FormData(this);

        try {
          const response = await fetch('/admin/upload-voters', {
            method: 'POST',
            body: formData,
          });

          const data = await response.json();

          if (data.success) {
            showSuccessNotification(data.message || 'Voters uploaded successfully');
            if (data.details && data.details.errorCount > 0) {
              console.warn('Upload errors:', data.details.errors);
            }
            setTimeout(() => {
              removeUploadVotersForm();
              window.location.reload();
            }, 2000);
          } else {
            showErrorNotification(data.error || 'Failed to upload voters');
            submitBtn.disabled = false;
            submitBtn.innerHTML = originalHTML;
          }
        } catch (error) {
          console.error('Error:', error);
          showErrorNotification('Network error. Please check your connection and try again.');
          submitBtn.disabled = false;
          submitBtn.innerHTML = originalHTML;
        }
      });
    </script> -->
  </body>
</html>